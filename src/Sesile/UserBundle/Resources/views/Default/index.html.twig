{% extends "SesileMainBundle:Default:index.html.twig" %}

{% block content %}
    <div class="bloc_page">
        <table class="table tableau">
            <thead>
                <tr>
                    <th>Mail</th>
                    <th>Nom</th>
                    <th>Actif</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for user in users %}
                    <tr class="user-row" data-id="{{ user.id }}">
                        <td>{{ user.username }}</td>
                        <td>{{ user.Nom }} {{ user.Prenom }}</td>
                        <td><img src="{% if user.enabled %}oui{% else %}non{% endif %}"/></td>
                        <td class="boutons_action">
                            <a href="{{ path("classeur_edit", {'id' : user.id }) }}">
                                <span class="glyphicon glyphicon-pencil"></span>
                            </a>
                            <a href="{{ path("classeur_edit", {'id' : user.id }) }}">
                                <span class="glyphicon glyphicon-trash"></span>
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <a href="{{ path("ajout_user") }}">Ajouter un utilisateur</a>
        <script type="text/javascript">
            $(".user-row").click(function () {
                document.location.href = Routing.generate('user_edit', { id: $(this).data("id") })
            });
        </script>
    </div>
{% endblock %}