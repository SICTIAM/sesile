imports:
    - { resource: parameters.yml }
    - { resource: security.yml }

framework:
    #esi:             ~
    translator:      { fallback: %locale% }
    secret:          %secret%
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_proxies: ~
    session:         ~
    fragments:       ~
    http_method_override: true
    session:
            save_path: "%kernel.root_dir%/sessions/"

# Twig Configuration
twig:
    debug:            %kernel.debug%
    strict_variables: %kernel.debug%
    form:
        resources:
            - 'SesileMainBundle:Form:fields.html.twig'
    globals:
        upload_path: uploads/avatars/
        upload_path_logo: uploads/logo_coll/
# Assetic Configuration
assetic:
    debug:          %kernel.debug%
    use_controller: false
    bundles:        [ SesileMainBundle ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: %kernel.root_dir%/Resources/java/compiler.jar
        #yui_css:
        #    jar: %kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar

# Doctrine Configuration
doctrine:
    dbal:
        driver:   %database_driver%
        host:     %database_host%
        port:     %database_port%
        dbname:   %database_name%
        user:     %database_user%
        password: %database_password%
        charset:  UTF8
        # if using pdo_sqlite as your database driver, add the path in parameters.yml
        # e.g. database_path: %kernel.root_dir%/data/data.db3
        # path:     %database_path%

    orm:
        auto_generate_proxy_classes: %kernel.debug%
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: smtp
    host:      servermail2.sictiam.fr
    username:  r.lasry@sictiam.fr
    password:  S|cti@m06
    spool:
            type: file
            path: "%kernel.root_dir%/spool"

oneup_uploader:
    mappings:
        docs:
            frontend: custom
            custom_frontend:
                class: Sesile\DocumentBundle\Controller\DocumentUploader
                name: DocumentUploader
            use_orphanage: true


fos_user:
    db_driver:     orm
    firewall_name: main
    user_class:    Sesile\UserBundle\Entity\User
    profile:
        form:
            type: sesile_user_profile


parameters:
    cas_server: 172.17.100.43
    cas_port: 389
    cas_path: /
    cas_protocol: '2.0'
    gorg:
        path: %kernel.root_dir%/../vendor/gorg/phpcas/
    upload:
        path: %kernel.root_dir%/../web/uploads/avatars/
        logo_coll: %kernel.root_dir%/../web/uploads/logo_coll/
        msg_acc: %kernel.root_dir%/../web/uploads/documents/msg_accueil.txt
    ldap:
        dn_admin: cn=admin,dc=sictiam,dc=fr
        dn_user: cn=Users,dc=sictiam,dc=fr
        password: oP1U88zz



jms_di_extra:
    locations:
        all_bundles: true

