imports:
    - { resource: config.yml }

#framework:
#    validation:
#        cache: apc

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc



parameters:
    database_driver: pdo_mysql
    database_host: 127.0.0.1
    database_port: null
    database_name: sesileprod
    database_user: root
    database_password: ***REMOVED***
    secret: ***REMOVED***
    cas_server: sso.sictiam.fr
    cas_port: 389
    cas_path: /
    cas_protocol: '2.0'
    ldap:
        dn_admin: cn=admin,dc=sictiam,dc=fr
        dn_user: cn=Users,dc=sictiam,dc=fr
        password: oP1U88zz
    domain_parse:
        default: sesile
        dbname: sictiam
    url_applet: signature.sesile.fr
    ovh:
        environnement: ""
        zone: sesile.fr
        target: sesile.fr.
    email_sender_address: sictiam@sesile.fr
    url_applet: signature.sesile.fr
    upload:
        path: %kernel.root_dir%/../web/uploads/avatars/
        fics: %kernel.root_dir%/../web/uploads/docs/
        logo_coll: %kernel.root_dir%/../web/uploads/logo_coll/
        msg_acc: %kernel.root_dir%/../web/uploads/documents/msg_accueil.txt
        signatures: %kernel.root_dir%/../web/uploads/signatures/
    majorversion: 3
    ovh_credential:
        application: ***REMOVED***
        secret: ***REMOVED***
        consumer_key: ***REMOVED***

security:

    firewalls:
        dev:
            path:  ^/(_(profiler|wdt)|css|images|js)/
            security: false
        secured_area:
            path:    ^/
            anonymous: true
            provider: webservice
            logout:
                success_handler: security.logout.success_handler
            cas:
                cas_server: sso.sictiam.fr
                cas_port: 443
                cas_path: /
                ca_cert_path: ~
                cas_protocol: '2.0'
                cas_mapping_attribute:
                check_path: /login_check